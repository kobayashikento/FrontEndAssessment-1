import React from 'react';

const Preloader = () => {
    return (
        <div style={{background: "#0B0B0B", height: "100vh", width: "100vw"}}>

        </div>
    )
}

export default React.memo(Preloader);